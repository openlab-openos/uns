# Openverse Domain System

A decentralized domain name system on Openverse.

## Features
- Multi-level domains (a.b.ov)
- JSON configuration
- Renewal & leasing
- Owner domain listing
- REST API resolution

## Setup
1. `npm install`
2. `anchor build`
3. `anchor deploy`
4. Run scripts in `scripts/`

